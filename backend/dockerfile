# Gradle 빌드는 CI에서 네이티브로 미리 수행하고(app.jar로 복사), 여기서는
# 그 결과물만 이미지에 담는다. JVM 바이트코드는 아키텍처 무관이라 amd64/arm64
# 멀티아키텍처 빌드 시 QEMU 에뮬레이션 위에서 무거운 컴파일을 돌릴 필요가 없어짐.
FROM eclipse-temurin:17-jdk
WORKDIR /app

COPY app.jar app.jar

EXPOSE 8080
ENTRYPOINT ["sh","-c","java $JAVA_OPTS -jar app.jar"]
